[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ysawa0-precommit-hooks"
version = "1.0.1"
description = "Custom pre-commit hooks for ysawa0 projects"
requires-python = ">=3.9"

[tool.setuptools]
packages = []
