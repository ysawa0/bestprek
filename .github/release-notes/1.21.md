## Remove YAML and variable-brace style checks

- Remove `check-yaml` from the repository and example configs so multi-document Kubernetes manifests are accepted.
- Disable ShellCheck SC2250 in the published hook defaults and example config. Quoted variables can use `$name` without braces.
- Add an end-to-end ShellCheck fixture with all optional checks enabled to verify the SC2250 exclusion.
