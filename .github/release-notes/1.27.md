## Unslop defaults

- Disable em-dash density, three-item-list density, question density, question-answer turns, and short-sentence runs in the recommended preset.
- Keep these checks available as informational suggestions in strict mode.
- Fail on warnings and errors by default, rather than informational findings. Explicit `fail_level` settings still apply.
- Update example configurations to use the recommended preset and warning failure threshold.
- Add CLI regression coverage for interview questions, reference headings, source metadata, and legitimate lists.

## Code checks

- Disable Oxlint's curly-brace rule.
- Reduce complexity in the Unbold scanner.
