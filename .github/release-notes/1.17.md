## Unslop fails on every finding

Release 1.17 changes Unslop's default failure threshold from `warning` to `info`.

Every emitted diagnostic now fails the hook. Rules configured as `off` remain silent, and `fail_level` can still override the default.
