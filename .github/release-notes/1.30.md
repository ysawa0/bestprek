## Go lint policy

- Expand the supplied Revive config to flag redundant control flow, duplicated branches and conditions, unnecessary formatting and statements, defer mistakes, value-receiver mutations, and unhandled errors.
- Limit cognitive complexity to 15, alongside the existing nesting limit of three.
- Add rejection fixtures for each new rule. Copy the updated `example_conf/revive.toml` when upgrading.
